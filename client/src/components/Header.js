import React from 'react';
import { Typography } from '@material-ui/core';

export const Header = () => {
  return (
    <Typography variant="h4"  gutterBottom>
        Expense Tracker
    </Typography>
  );
}
